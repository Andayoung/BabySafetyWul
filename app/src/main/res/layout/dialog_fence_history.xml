<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_fence_operate"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentBottom="true"
    android:background="@drawable/all_corners_bg"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <include layout="@layout/dialog_top" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="300dp">
        <TextView
            android:id="@+id/txt_history"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="暂无记录"
            android:gravity="center"
            android:textColor="#000000"
            android:layout_margin="20dp"/>
    </ScrollView>
    <Button
        android:id="@+id/btn_fenceOperate_cancel"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_marginBottom="5dp"
        android:layout_marginTop="5dp"
        android:background="@mipmap/bg_btn_common"
        android:text="@string/all_cancel"
        android:textColor="@color/layout_title"
        android:textSize="15dp" />


</LinearLayout>